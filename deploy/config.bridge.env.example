# ========== 行情 bridge（可选）==========
# 复制为 deploy/config.bridge.env
# 不创建此文件时，bridge 使用 config.accountA.env 里的 MT5_PATH / MT5_TCP_HOST

MT5_PATH=C:\MT5-A\terminal64.exe
MT5_TCP_HOST=43.99.61.45
MT5_TCP_PORT=9627
MT5_SYMBOLS=XAUUSD,XAGUSD,USDCNH
MT5_POLL_MS=200
